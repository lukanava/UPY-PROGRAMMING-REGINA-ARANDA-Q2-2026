Cuando se repite codigo es un  codigo fragil  
las funciones permiten usarlas cuando se necesiten, es decir son bloqes de codigo reusables
las variables deben ser autodescriotivas, deben empezar con un verbo
la logica no cambia
Defining and calling

usamos def para delcarar una funcion, seguido por el verbo y parametros(variables que recibe la funcion)
en el cuerpo ponemos todo el codigo que queremos que haga lla ffuncion, usamos 4 espacios para eso
return es opcional, depende de que  quires que haga

una funncion que realiza accion pero no regresa valores no  tiene  return
return se usa para calculos

los arguumentos son mapeados por poscicion de izquierda a derecha el orden si importa
si lo pongo nombre=*** ya  no importa el orden y ya no da error  
tambien se pueden definir valores por default
se puede regresar mas de un valor, usandoo  ccoas,liasta,arreglos, dicionarios,etc.
para evitar errores si no hay un valor usamos un  if antes de la realizacion de la operacion

SCOPE
se define como el lugar donde python buscara la variable
la funcion vive y muere en una sola linea 
muy importante la identacion 
variable global pegada a la izquierda
local viene dentro de una funcion 

LAMBDA EXPRESSIONS
lambda es una funcion anonima que se define ne una sola linea
se pueden encadenar facilmente 
son anonimas, son una expresion de una sola linea y el return es implicito
lambda parameters: expression
lambda debe evualar algo
usamoa lambda cuando se puede pasar como argumento y se hace una sola vez
map retorna un objeto 
piensalo como aplica esta tranfoormcacion a cada elemento 

FILTER
solo regresa los elementos donde la funcion sea verdadera
solo se queda con los items que sarisfacen la condicion 

MAP
transforma cada elemento
su tamaño de salida es igual al imput
regresa los valores

