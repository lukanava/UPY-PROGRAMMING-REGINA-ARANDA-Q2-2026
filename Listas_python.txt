arrays en plural
posicion -1
list, tuple,dictionary, set 
ordenado se refiere a que como acomodo los valores estara la lista
se pueden cambiar los datos (mutables)
puedo meter el mismo elemento mas de una vez

Tuplas:
no son mutables no cambian 


Diccionarios:
No hay duplicados 

Set:
no ordenado y no duplicados 

LISTAS:
indexing podemos accesar los elementoos de acuerdo a su posicion 
usamos el negativo para refereinrnos a elementos que estan en los extremos 
sublistas o slicing {donde empieza,donnde termina,cuantos pasos}
lista inversa {::-1}
modificar indicando la posicion a remplazar
grades[1]=8.0
append para agregar nuevos elementos (+= con ia tener cuidado) lo agrega al final
insert lo agrega en ela posicion que indiques 
remove solo quita el primer elemento de izquierda a derecha 
pop remueve el ultimmo elemnento, podemos guardar ese elemento en una variable
len cuantos elementos hay 
boleanos para saber si el elemento esta en la lista 
count para contar cuandos elementos de esos hay 
sort ordena  del menor al  mayor 
reverse=true (mayor a menor)
trabajr con copias de los datos, sorted (original)
son itereables, puedo hhacer operaciones
enumerate toma los elementos de la lista y te da la posicion

TUPLAS:
son estructuras de dator ordenados e inmutables 
usar en cordenadas, colores,meses
posiscioni igul que en las listas
soportan las mimas operaciones que las listas, menos la modificacion 
pasa algo  si se modifica este dato?? si si, usar tupla por seguridad


DICIONARIOS:
trabajar con diccionarios y json es lo mismo practicamente
crear un diccionario es llave,string y valores
usamos el get para una key no excitente
diccionario{elemento}
keys te regresa las lstas del diccionario 
values les regresa una lisya de todos los elementos 
items te da ....
iteramos  usando items 
lista de diccionario 


SETS:
colecciones de elementos unicos
elimina las copias
para un set vacio es set() no {}
lo usamos cuanndo no queremos duplicidad
usamos tags.add, discard y remove 
remove->el ele,ento no se encuentra tira error 
discard->no da error (usar para remover elementos a menos de que quieras especificamennte el erro)
interseccion para encontar elementos en comnn de colecciones 
son como los diagramas de ven 
